# Task List — <Feature Name>

> Generated by the **Slice Planning** stage.
> Each task is one independently deliverable vertical slice (Data → Domain → UI → Tests).
> Tasks must be implemented in order. Do not start a task until the previous one is merged and green.

---

## Slicing Strategy

> State which strategy was applied: Vertical Slices / Contract-First / Risk-First.
> 1–2 sentences explaining why this order was chosen.

---

## Tasks

### Task 1 — <Short Name>

#### Objective

<What this task accomplishes, e.g., "Allowing the user to toggle favorite status on a note from the notes list.">

#### User-visible behavior

- <Observable UI element, styling, or text changes>
- <Existing active/default behavior that must remain unchanged>

#### Scope

May touch:
- <e.g., note DTO/model if required>
- <e.g., note mapper>
- <e.g., note UI model>
- <e.g., note list ViewModel if required>
- <e.g., note item SwiftUI View>
- <e.g., mapper, ViewModel, or SwiftUI UI tests>

#### Required tests

| ID | Given | When | Then | Type |
|---|---|---|---|---|
| T1.1 | <precondition> | <action> | <expected outcome> | Unit / ViewModel unit / SwiftUI UI |

#### Done criteria

- <Condition to prove task works end-to-end, e.g., "Favorite status is successfully toggled and updated in the DB and UI.">
- <Verification criteria, e.g., "Required tests pass.">
- <Boundary constraint, e.g., "No unrelated folder/search/settings files changed.">

---

### Task 2 — <Short Name>

#### Objective

<What this task accomplishes>

#### User-visible behavior

- <Observable UI element, styling, or text changes>
- <Existing active/default behavior that must remain unchanged>

#### Scope

May touch:
- <e.g., DTO/model changes>
- <e.g., Repository interface and implementation>
- <e.g., UI model and screen SwiftUI Views>
- <e.g., Mapper, ViewModel, or SwiftUI UI tests>

#### Required tests

| ID | Given | When | Then | Type |
|---|---|---|---|---|
| T2.1 | <precondition> | <action> | <expected outcome> | Unit / ViewModel unit / SwiftUI UI |

#### Done criteria

- <Condition to prove task works end-to-end>
- <Verification criteria>
- <Boundary constraint>

---

<!-- Repeat the Task block above for each additional slice -->

---

## Dependency Order

```
Task 1 ──→ Task 2 ──→ Task 3 ──→ ...
```

> If tasks can run in parallel, show branches. Otherwise keep it linear.
