struct CommentOnly: View {
    var body: some View {
        Text(NotesCopy.title)
    }
}

// Button("Tap"); Color.red; VStack { ForEach(items) { _ in EmptyView() } }
let sourceExample = "Text(\"Hardcoded\")"
